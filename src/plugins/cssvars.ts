// const plugin = { id: 'cssvars' }

// plugin.code = async () => {

//     // get css vars
//     // for(let [k,v] of Object.entries(cfg.data)) if(k.startsWith('--')) cssvars.push([k, v])
//     // // apply css vars
//     // cssvars.map(css => document.documentElement.style.setProperty(css[0], css[1]))

// }

// export default plugin